<h2>Enter Post</h2>

<%= form_for @post do |f| %>
	<% if @post.errors.any? %>
		<div id="error_explination">
			<h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
			<ul>
				<% @post.errors.full_messages.each do |msg| %>
					<li> <%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<%= f.text_area :content, class: "form_control", rows: "8" %>
	<%= f.submit "Sumbit Post", class: "btn btn-large btn-primary" %>

<% end %>
